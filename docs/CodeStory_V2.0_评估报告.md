# CodeStory V2.0 正式评估报告

> 状态：有条件发布（仅发布候选）；生产迁移与人工质量门禁待完成
>
> 生成日期：2026-08-07

## 版本冻结

| 项目 | 冻结值 |
| --- | --- |
| 对话模型 | `Qwen/Qwen3-30B-A3B-Instruct-2507` |
| Tutor Prompt | `grounded-v3`（内部修订 `grounded-v3.1-boundary`） |
| 出题 Prompt | `exercise-gen-v2` |
| Embedding | `Qwen/Qwen3-Embedding-4B`，1024 维 |
| RAG 数据集 | `rag-v1-candidates`，40 条回答集 |
| 出题数据集 | `stage2-formal-qwen30b-rerun-20260803`，50 条 |
| 评分数据集 | `grading-stage3-2026-08-01`，30 条 |
| 学习图 | `guided-learning-v1` |

切换模型、Prompt、维度或数据集后，本报告结论失效；Embedding 切换还必须全量
重建索引。

## 当前证据与缺口

| 阶段 | 已有证据 | 未关闭门禁 |
| --- | --- | --- |
| 1 RAG | 40 条 Qwen3 回答、claim 机器/Agent 支持率通过；Recall@5 为 1.00 | 独立人工支持准确率 `≥ 90%` |
| 2 出题 | 正式 50 题结构成功 `50/50`、最终成功 `50/50` | 人工答案正确率 `≥ 95%`、重复率 `≤ 5%` |
| 3 评分 | 30 条 Qwen3 Agent 基准一致率 `96.67%`、MAE `9.67` | 独立人工一致率 `≥ 90%`、MAE `≤ 10` |
| 4 状态机 | checkpoint 恢复、CAS、effect 幂等、故障注入及移动端证据已具备；工程审核通过 | 生产开关继续保持关闭，待 0C 后冒烟 |
| 5 推荐 | 离线规则夹具通过 | 真实曝光达到 100 后统计点击率和完成率 |
| 6 治理 | 调用日志、报告、限流、RAG/checkpoint/日志维护能力完成整合并通过独立审查 | 流式 usage 供应商实测与生产演练 |

人工工作表由 `pnpm run eval:prepare-human-review` 生成，所有人工字段默认空白，
不得把 Agent 标签计入人工分母。失败样本必须原样保留。至少 10 个失败案例的正式
分类将在完整人工审核与生产冒烟完成后填写；当前不虚构失败案例。

## 工程整合门禁（2026-08-07）

- 后端 build、四项认证检查通过；17 条 migration 从空临时库执行成功。
- 后端完整测试为 24 个文件、188 个用例全部通过；测试结束后临时库已删除。
- 运维脚本 TypeScript 检查和 4 条纯逻辑测试通过；AI 报告、RAG 维护、checkpoint
  清理、调用日志清理均在完整迁移的临时库完成默认 dry-run。
- 前端 typecheck、ESLint、生产构建通过；阶段 2 Playwright 3 / 3、阶段 5
  Playwright 2 / 2 通过。
- 独立审查结论为“条件通过”：上一轮发现的 Embedding 崩溃、指标缺数据误判、
  RAG generation 竞态、选择题误限流、feedback/trace 断链和历史 checkpoint
  生命周期问题均已关闭，未发现新的确定性功能回归。

自由 Chat 当前未启用供应商流式 usage，因此存在真实 Chat 流量时，token/成本覆盖
不足会诚实返回 `INSUFFICIENT_DATA`，不能把预算门禁标记为通过。生产候选必须先
验证供应商是否支持最终 chunk usage；不支持时继续维持“有条件发布”。

## 发布判断

当前结论为 **有条件发布**，含义仅是完成工程整合后可形成发布候选，不代表已生产
上线。阻塞项为阶段 1～3 独立人工质量门禁、流式 usage 供应商实测、0C 生产迁移/
恢复演练和阶段 5 的 100 次真实曝光效果验证。阻塞项关闭前，生产 RAG 与学习图
开关保持关闭。

复验命令与运维门槛见《CodeStory V2.0 AI 运维手册》；每次正式复验须记录命令、
提交 hash、环境、完整原始结果和审核人。

