# CodeStory V2.0 推荐评估报告

> 日期：2026-08-04
> 状态：工程门禁与离线规则夹具通过

## 评估口径

- 固定集：`backend/evals/datasets/recommendation-stage5.ts`，共 10 条，覆盖错题、
  薄弱掌握度、冷启动、同课程相关小节、RAG 降级和审核态隔离。
- 标签来源：`spec-derived-review`，由阶段 5 任务书的明确业务规则推导，不冒充
  线上人工行为数据。
- 指标：宏平均 Precision@5，门禁为 `≥ 0.70`；少于 5 条或重复推荐均按缺位
  计入分母。

## 实测

运行：

```bash
cd backend
pnpm run eval:recommendations
```

结果：

- 样本数：10；
- Precision@5：0.80；
- 门禁：通过；
- 报告：`backend/evals/reports/recommendation-stage5.json`。

数据库集成测试另外覆盖当前用户数据隔离、冷启动、RAG 失败回退、当前小节排除、
埋点幂等、跨用户 token 拒绝和错误状态映射。2026-08-04 使用本地独立临时库
从空库执行 14 条迁移，完整后端 19 个测试文件 / 159 个用例、其中新增 6 个
阶段 5 集成用例全部通过，teardown 已删除临时库。前端 Playwright 2 / 2 通过，
覆盖曝光/点击、推荐失败非阻塞和 390px 无横向溢出。

## 限制

该报告验证的是排序规则和工程行为，不声明线上推荐效果提升。点击率和复习完成率
必须在曝光达到 100 次后报告样本量；当前不设置或伪造线上百分比。
